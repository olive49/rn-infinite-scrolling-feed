import { memo } from "react";
import { View, Text } from "react-native";

const FeedItem = () => {
    return (
        <View>
        <Text>Feed Item</Text>
        </View>
    );
}

export default memo(FeedItem)